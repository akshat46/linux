
total_exits:
#193        vmx.c: variable definitions
#10047      vmx.c: incremented by 1 everytime vmx_handle_exit variable is entered
#32         cpuid.c: importing as extern from vmx.c

###current###
#988		//cpuid.c: instead of int, used double in printk (replaced with pr_info)
#1005		//cpuid.c: same as line 988
#987		assigning total_exits_temp to a temp int (without casting to unsigned long)
#m-l		(multiple lines in vmx_handle_exit inside vmx.c) used rdtsc() instead of clock()

